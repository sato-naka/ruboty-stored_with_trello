require "spec_helper"

RSpec.describe Ruboty::StoredWithTrello do
  it "has a version number" do
    expect(Ruboty::StoredWithTrello::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
